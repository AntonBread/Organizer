package com.app.organizer.note.factory.abstr;

import android.util.Log;

public class GoalsUIElement implements UIElement {
    @Override
    public void doSomething() {
        Log.d("TEST", "This is a message from Goals UI Element!");
    }
}
